# deploy.sh configuration
# Copy this to .deploy.env and fill in your values.
# .deploy.env is gitignored — your config stays local.

MC_HOST=your-mcbox-hostname    # e.g., atari400.local, 192.168.1.100
MC_USER=motleycrew             # SSH user on mcbox
MC_REPO_PATH=~/Documents/motley-crew  # path to motley-crew repo on mcbox
# MC_BRANCH=main               # optional: branch to pull (default: current)
